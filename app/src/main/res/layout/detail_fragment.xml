<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/spacing_detail"
    android:orientation="vertical"
    tools:context=".ui.detail.DetailFragment">

    <TextView
        android:id="@+id/title"
        style="@style/title_bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ImageView
        android:id="@+id/image_cartoon"
        android:layout_width="@dimen/detail_image_dimen"
        android:layout_height="@dimen/detail_image_dimen"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="@dimen/spacing_detail"
        android:contentDescription="@string/app_name" />

    <TextView
        android:id="@+id/detail"
        style="@style/detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_detail" />

</LinearLayout>